/**
 * ========== 구현할 메서드 (매일 지우고 새로 구현 연습) ==========
 *
 *   heapify(i)   buildHeap(arr)   sort(arr)
 *
 * 위 3개만 구현하고 테스트한다. 나머지 메서드는 이미 제공됨.
 *
 * ---------------------------------------------------------------------------
 * 인덱스 규칙 (0-based)
 * ---------------------------------------------------------------------------
 *   루트 = heap[0]
 *   left(i)  = 2*i + 1    right(i) = 2*i + 2    parent(i) = floor((i-1)/2)
 *   마지막 비리프 인덱스 = floor(n/2) - 1
 *
 * ---------------------------------------------------------------------------
 * 1. heapify(i)
 * ---------------------------------------------------------------------------
 *   - 시그니처: heapify(i: number): void
 *   - 전제: i의 왼쪽·오른쪽 자식 서브트리는 이미 힙이다.
 *   - 동작: i를 루트로 하는 서브트리가 힙이 되도록, i에서부터 아래로 내려가며

 * ---------------------------------------------------------------------------
 * 2. buildHeap(arr)
 * ---------------------------------------------------------------------------
 *   - 시그니처: buildHeap(arr: number[]): this (또는 void)

 * ---------------------------------------------------------------------------
 * 3. sort(arr)
 * ---------------------------------------------------------------------------
 *   - 시그니처: sort(arr: number[]): number[]
 *   - 반환: 오름차순 정렬된 새 배열. 인자 arr은 수정하지 않음.
 *
 * =============================================================