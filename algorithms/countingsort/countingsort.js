/**
 * ========== 구현할 함수 (매일 지우고 새로 구현 연습) ==========
 *
 *   sort(arr)
 *
 * ---------------------------------------------------------------------------
 * 카운팅 소트 (Counting Sort)
 * ---------------------------------------------------------------------------
 * - 비교 정렬이 아님: 원소 비교 없이 각 값의 출현 횟수로 정렬.
 * - 전제: 원소는 정수이며, 범위 [min, max]를 알 수 있음 (배열에서 min/max 추론).

 * ---------------------------------------------------------------------------
 * sort(arr)
 * ---------------------------------------------------------------------------
 * - 시그니처: sort(arr: number[]): number[]
 * - 반환: 오름차순 정렬된 새 배열 (정수만 가정).
 * - 인자 arr은 수정하지 않음 (원본 불변).
 * - 빈 배열이면 [] 반환.
 *
 * =============================================================
 */
